# Build artifacts
/agentboard
/dist/
frontend/dist/
/node_modules/
frontend/node_modules/
frontend/tsconfig.tsbuildinfo
landing/dist/
landing/node_modules/
landing/.astro/
*.tsbuildinfo

# Runtime / caches
/.task/
/.gstack/
.agentboard/
*.sqlite
*.sqlite3
*.db

# Editor / OS
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Machine-local Claude Code config and installed skills
/.claude/
# …but ship the project-specific agentboard dogfood skill so the invariant
# (port 3000, agentboard-dev project, feature-page template) travels with the repo.
!/.claude/
!/.claude/skills/
/.claude/skills/*
!/.claude/skills/agentboard/
!/.claude/skills/agentboard/**
/.claude/settings.local.json

# test_claude playground — runtime artifacts (scaffolding is tracked)
test_claude/bin/
test_claude/agentboard-data/
test_claude/.agentboard/

# Logs
*.log
.gstack/

# Bruno dotenv — holds the hosted-instance auth token (see bruno/.env.example)
bruno/.env

# Local dogfood / dev tokens — never commit
.tokens.local
*.token.local
