AgentBoard is a workspace at <CLONE_URL>. Your current directory IS the workspace — don't clone it somewhere else. Wire this directory to the remote:

  git init -q
  git remote add origin https://_:<TOKEN>@<HOST>/git/<WORKSPACE>.git
  git fetch -q origin
  git checkout -B main origin/main

Now README.md is right here in your cwd. Read it — it points you at skills/agentboard/SKILL.md which teaches the protocol (auth, the git-vs-MCP fork, conflict resolution, conventions). From that point on AgentBoard is the source of truth; my instructions stop here.

When you're done reading your way in, tell me:
 1. What kind of workspace this is.
 2. What open work it surfaces.
 3. Whether you'd contribute via git or via the MCP fallback (the four agentboard_* tools), and why.
 4. What you'd do first if I asked you to ship something here.
