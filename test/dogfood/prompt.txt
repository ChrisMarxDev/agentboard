AgentBoard is a workspace at __BASE_URL__. Your current directory IS the workspace — wire it to the remote here, don't clone elsewhere:

  git init -q
  git remote add origin http://_:__TOKEN__@__HOSTPORT__/git/dogfood.git
  git fetch -q origin
  git checkout -B main origin/main

Then read README.md at the root, and do exactly what it tells you to do. The workspace is the source of truth for how to use AgentBoard — your instructions come from there, not from me. When you're done with whatever the README sends you off to read, summarize what you learned and what you did so the test harness can verify the bootstrap worked.

You have Bash, Read, and Write tools available. Use them.
