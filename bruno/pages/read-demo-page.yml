info:
  name: Read /demo page source
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/pages/demo"
  headers:
    - name: Accept
      value: text/markdown

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
