info:
  name: 5. MERGE by id (deep merge item)
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/data/demo.items/abc"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "qty": 99
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
