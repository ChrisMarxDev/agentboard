info:
  name: 3. APPEND (push onto array)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/data/demo.log"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "msg": "first entry",
        "level": "info"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
