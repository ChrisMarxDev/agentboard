info:
  name: 4. UPSERT by id (create-or-replace item)
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/data/demo.items/abc"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "first item",
        "qty": 1
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
