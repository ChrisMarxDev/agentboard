info:
  name: Seed Table rows
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/data/demo.rows"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      [
        { "id": "u1", "name": "Alice", "role": "Engineer", "active": true },
        { "id": "u2", "name": "Bob",   "role": "Designer", "active": true },
        { "id": "u3", "name": "Carol", "role": "PM",       "active": false }
      ]

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
