info:
  name: Append Log entry (run repeatedly to see log scroll)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/data/demo.events"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "level": "info",
        "msg": "Hello from Bruno",
        "ts": "2026-04-17T12:00:00Z"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
