info:
  name: Reject invalid component name (../etc/passwd)
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/components/..%2Fetc%2Fpasswd"
  headers:
    - name: Content-Type
      value: application/javascript
  body:
    type: text
    data: |-
      export default () => null

runtime:
  scripts:
    - type: tests
      code: |-
        test("rejects path-traversal name with 400", function() {
          // Expect either 400 (validation) or 404 (chi rejected the path entirely)
          expect([400, 404]).to.include(res.status);
        });
