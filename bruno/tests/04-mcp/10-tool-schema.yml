info:
  name: tools/call agentboard_get_data_schema
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/mcp"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {"jsonrpc":"2.0","id":12,"method":"tools/call","params":{"name":"agentboard_get_data_schema","arguments":{}}}

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() { expect(res.status).to.equal(200); });
        test("content is parseable JSON object", function() {
          const text = res.body.result.content[0].text;
          const parsed = JSON.parse(text);
          expect(parsed).to.be.an("object");
        });
