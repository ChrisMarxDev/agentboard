info:
  name: SET object
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/data/test.obj"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      { "a": 1, "b": 2, "nested": { "x": "y" } }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() { expect(res.status).to.equal(200); });
