info:
  name: SET scalar value
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseUrl}}/api/data/test.scalar"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      42

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() { expect(res.status).to.equal(200); });
        test("response has ok:true", function() { expect(res.body.ok).to.equal(true); });
