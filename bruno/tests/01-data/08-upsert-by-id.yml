info:
  name: UPSERT by id creates item
  type: http
  seq: 8

http:
  method: PUT
  url: "{{baseUrl}}/api/data/test.items/abc"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      { "name": "first", "qty": 1 }

runtime:
  scripts:
    - type: tests
      code: |-
        const axios = require('axios');
        test("status 200", function() { expect(res.status).to.equal(200); });
        test("item persisted with id", async function() {
          const r = await axios.get(`${bru.getEnvVar("baseUrl")}/api/data/test.items/abc`);
          expect(r.data.value.id).to.equal("abc");
          expect(r.data.value.name).to.equal("first");
          expect(r.data.value.qty).to.equal(1);
        });
