info:
  name: GET /api/data/schema returns a JSON object
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/api/data/schema"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() { expect(res.status).to.equal(200); });
        test("returns an object", function() {
          expect(res.body).to.be.an("object");
        });
