info:
  name: GET missing key returns 404
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/api/data/test.does-not-exist"

settings:
  timeout: 0

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 404", function() { expect(res.status).to.equal(404); });
        test("error body has code", function() {
          expect(res.body.code).to.exist;
        });
