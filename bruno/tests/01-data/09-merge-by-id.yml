info:
  name: MERGE by id deep-merges item
  type: http
  seq: 9

http:
  method: PATCH
  url: "{{baseUrl}}/api/data/test.items/abc"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      { "qty": 99 }

runtime:
  scripts:
    - type: tests
      code: |-
        const axios = require('axios');
        test("status 200", function() { expect(res.status).to.equal(200); });
        test("qty updated, name preserved", async function() {
          const r = await axios.get(`${bru.getEnvVar("baseUrl")}/api/data/test.items/abc`);
          expect(r.data.value.qty).to.equal(99);
          expect(r.data.value.name).to.equal("first");
        });
