info:
  name: APPEND extends existing array
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/data/test.log"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      { "msg": "second", "level": "warn" }

runtime:
  scripts:
    - type: tests
      code: |-
        const axios = require('axios');
        test("status 200", function() { expect(res.status).to.equal(200); });
        test("array grew to 2", async function() {
          const r = await axios.get(`${bru.getEnvVar("baseUrl")}/api/data/test.log`);
          expect(r.data.value.length).to.equal(2);
        });
