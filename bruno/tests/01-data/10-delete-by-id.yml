info:
  name: DELETE by id removes item from collection
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{baseUrl}}/api/data/test.items/abc"

runtime:
  scripts:
    - type: tests
      code: |-
        const axios = require('axios');
        test("status 200", function() { expect(res.status).to.equal(200); });
        test("GET after delete is 404", async function() {
          const r = await axios.get(`${bru.getEnvVar("baseUrl")}/api/data/test.items/abc`, { validateStatus: () => true });
          expect(r.status).to.equal(404);
        });
