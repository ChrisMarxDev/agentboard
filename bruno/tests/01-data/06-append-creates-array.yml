info:
  name: APPEND to missing key creates array
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/data/test.log"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      { "msg": "first", "level": "info" }

runtime:
  scripts:
    - type: tests
      code: |-
        const axios = require('axios');
        test("status 200", function() { expect(res.status).to.equal(200); });
        test("value is array of length 1", async function() {
          const r = await axios.get(`${bru.getEnvVar("baseUrl")}/api/data/test.log`);
          expect(r.data.value).to.be.an("array");
          expect(r.data.value.length).to.equal(1);
          expect(r.data.value[0].msg).to.equal("first");
        });
