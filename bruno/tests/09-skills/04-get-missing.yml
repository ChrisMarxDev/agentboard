info:
  name: GET /api/skills/:slug returns 404 for an unknown slug
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/skills/does-not-exist"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 404", function() { expect(res.status).to.equal(404); });
