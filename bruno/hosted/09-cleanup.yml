info:
  name: DELETE /api/data/demo.bruno.hosted (cleanup)
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/api/data/demo.bruno.hosted"
  headers:
    - name: Authorization
      value: "Bearer {{process.env.AGENTBOARD_AUTH_TOKEN}}"
      enabled: true

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
