info:
  name: Health is open (no auth required)
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/health"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
        test("ok is true", function() {
          expect(res.body.ok).to.equal(true);
        });
