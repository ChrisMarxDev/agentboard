info:
  name: Query-string ?token= also works (shareable link)
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/data?token={{process.env.AGENTBOARD_AUTH_TOKEN}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
