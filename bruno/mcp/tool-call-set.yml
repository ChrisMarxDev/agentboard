info:
  name: MCP tools/call agentboard_set
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/mcp"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "id": 3,
        "method": "tools/call",
        "params": {
          "name": "agentboard_set",
          "arguments": {
            "key": "demo.from_mcp",
            "value": { "msg": "set via MCP", "ts": "2026-04-17" }
          }
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
