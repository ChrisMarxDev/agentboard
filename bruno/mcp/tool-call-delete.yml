info:
  name: MCP tools/call agentboard_delete
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/mcp"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "id": 15,
        "method": "tools/call",
        "params": {
          "name": "agentboard_delete",
          "arguments": { "key": "demo.from_mcp" }
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
        test("no JSON-RPC error", function() {
          expect(res.body.error).to.be.undefined;
        });
