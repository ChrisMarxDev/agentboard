info:
  name: Add a new task (Kanban)
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/data/welcome.tasks"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id": "4",
        "title": "Ship the Bruno collection",
        "status": "todo"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
