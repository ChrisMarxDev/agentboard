info:
  name: Move task to done (Kanban — merge by id)
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/data/welcome.tasks/2"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "status": "done"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
