info:
  name: List pages
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/content"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
