info:
  name: GET unknown id in collection returns 404
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/data/welcome.tasks/999"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 404", function() {
          expect(res.status).to.equal(404);
        });
