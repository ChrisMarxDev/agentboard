info:
  name: Get components.js bundle
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/components.js"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
        test("serves JavaScript", function() {
          expect(res.headers["content-type"]).to.match(/javascript/);
        });
