info:
  name: List built-in components
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/components"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
