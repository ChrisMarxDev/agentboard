info:
  name: Get inferred JSON schema
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/data/schema"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
