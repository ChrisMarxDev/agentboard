info:
  name: List all keys
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/data"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
