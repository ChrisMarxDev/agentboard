info:
  name: Get welcome.tasks/1 (by ID)
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/data/welcome.tasks/1"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", function() {
          expect(res.status).to.equal(200);
        });
        test("returns the item with matching id", function() {
          expect(res.body.id).to.equal("1");
        });
